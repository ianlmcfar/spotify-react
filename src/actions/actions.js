export const KEY_DOWN = 'KEY_DOWN'

export function keyUp(index) {
  return {
    type: KEY_DOWN,
    count
  }
}

export const KEY_UP = 'KEY_UP'

export function keyDown(index) {
  return {
    type: KEY_UP,
    count
  }
}